import * as React from "react";

export const SdfDocumentCard: React.FunctionComponent<{
  id: string;
  schemas: {
    id: string;
    name: string;
  }[];
}> = ({id, schemas}) => {
  return <div></div>;
};
