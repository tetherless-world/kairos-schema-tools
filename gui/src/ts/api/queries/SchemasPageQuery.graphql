query SchemasPageQuery($sdfDocumentId: String!, $withSdfDocument: Boolean!) {
    schemas @skip(if: $withSdfDocument) {
        id
        name
        sdfDocumentId
    }

    sdfDocumentById(id: $sdfDocumentId) @include(if: $withSdfDocument) {
        name
        schemas {
            id
            name
        }
    }
}
