#import "./ValidationMessageFragment.graphql"

fragment SdfDocumentFragment on SdfDocument {
    name
    schemas {
        id
        name
    }
    sdfVersion
    sourceJson
    validationMessages {
        ... ValidationMessageFragment
    }
}
