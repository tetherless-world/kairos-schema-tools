#import "./JsonTokenLocationFragment.graphql"

fragment JsonNodeLocationFragment on JsonNodeLocation {
    startToken {
        ... JsonTokenLocationFragment
    }
    stopToken {
        ... JsonTokenLocationFragment
    }
}
