#import "./ValidationMessageFragment.graphql"

fragment SdfDocumentPageFragment on SdfDocument {
    id
    name
    schemas {
        id
        name
        sdfDocumentId
    }
    sdfVersion
    sourceJson
    validationMessages {
        ... ValidationMessageFragment
    }
}
