query SdfDocumentPageQuery($id: String!) {
    sdfDocumentById(id: $id) {
        id
        name
        schemas {
            id
            name
            sdfDocumentId
        }
        sdfVersion
        sourceJson
        validationMessages {
            ... ValidationMessageFragment
        }
    }
}
